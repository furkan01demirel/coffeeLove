[build]
  command = "npm run build"
  publish = "dist"

# Netlify -> Railway proxy
[[redirects]]
  from = "/api/*"
  to = "https://coffeelove-production.up.railway.app/api/:splat"
  status = 200
  force = true