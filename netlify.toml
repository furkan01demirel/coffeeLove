[build]
  command = "npm run build"
  publish = "dist"

# Netlify -> Render proxy
[[redirects]]
  from = "/api/*"
  to = "https://YOUR-RENDER-URL.onrender.com/api/:splat"
  status = 200
  force = true